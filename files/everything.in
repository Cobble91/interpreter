const a: int = 12;

fn main() {
  var x: int = 0;
  var b: bool = true;
  var s: string = "hello world";
  var f: float = -123.456;
  var y: int = my_func(x);
  while (x == y) {
    x = x + 1;
    my_func(x);
  }
}

fn my_func(x: int) -> int {
  var z: int = x * x;
  return z;
}
